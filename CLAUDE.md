# vt

This is the VT plugin repository. The delegation authorization below is the same text
shipped at `claude/instructions/delegation-mandate.md` — imported rather than copied so
the two can never drift.

@claude/instructions/delegation-mandate.md
